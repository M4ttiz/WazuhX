prima di eseguire il promt, crea un nuovo branch dove lavorerai.

[CONTESTO]
Stai lavorando esclusivamente sulla cartella `frontend/` del progetto WazuhX (React + Vite + Tailwind CSS).
NON devi assolutamente modificare:
- Qualsiasi file API (es. /api, /services, axios config, endpoint)
- File di configurazione Docker (docker-compose.yml, Dockerfile, env)
- Backend (Node.js, Express, routes, controller)
- Qualsiasi logica di autenticazione o gestione stato globale già funzionante (es. Redux/Context se già presenti)

Il tuo compito è **rifare completamente l'interfaccia utente** mantenendo:
- Le stesse pagine (Overview, Hosts, Services, Alerts, Inventory, Maps, Dashboards, Analytics, Metrics, Trends, Reports, Administration, Users, Roles, Settings, Integrations)
- Le stesse chiamate API esistenti (i nomi degli endpoint, i payload, i dati ricevuti non cambiano)

[OBIETTIVO PRINCIPALE]
Trasformare la UI attuale in una dashboard moderna, basata sul layout e sulle metriche mostrate nell'immagine "MISAT Monitor" (PNG allegato).  
I dati reali devono venire **dalle API già esistenti** di WazuhX, NON da mock fissi.

[LAYOUT DA RISPETTARE – derivato dal PNG]
1. **Header KPI** (Total Hosts, Up Hosts, Warning Hosts, Critical Hosts, Down Hosts, Total Services, Problems, Unchecked)
2. **Host Status Distribution** (grafico a torta o anello: OK, Warning, Down)
3. **Service Status Distribution** (OK, Warning, Critical, Unknown)
4. **Tabella Top Problem Hosts** (Host, Status, Problems, Last Change)
5. **Tabella pivot "Host Problems by State"** (righe: Critical/Warning/Unknown – colonne: numero e percentuale)
6. **Tabella pivot "Service Problems by State"**
7. **Sidebar di navigazione** con le voci esistenti (Overview, Hosts, Services, ... Integrations)

[VINCOLI TECNICI – SOLO UI]
- Utilizza i componenti già presenti in `frontend/src/components/` se riutilizzabili, altrimenti creane di nuovi dentro una cartella `components/dashboard/`
- Usa Tailwind CSS per lo stile (non toccare index.css se non per variabili tematiche globali)
- Per i grafici: installa e usa `recharts` (è già probabile che ci sia, altrimenti aggiungilo solo al frontend)
- Non alterare la logica di fetch esistente: se ora usi `useEffect` con `axios`, lascialo identico. Limiti a **visualizzare** i dati che arrivano.
- Gestisci stati di loading e errore con componenti UI (spinner, skeleton, messaggi) ma senza cambiare la logica di recupero dati.

[FLUSSO DI LAVORO PER CURSOR]
1. Analizza la struttura attuale della cartella `frontend/src`.
2. Identifica le pagine (Overview, Hosts, ecc.) e i loro componenti.
3. Per la pagina **Overview** (che è quella principale da cui parte il refactoring):
   - Sostituisci il contenuto esistente con il nuovo layout KPI + tabelle.
   - Usa gli stessi `useEffect` e chiamate API per popolare i dati (es. `/api/overview`, `/api/hosts`, `/api/services`, `/api/alerts/top`).
   - Trasforma i dati grezzi nelle strutture necessarie per le tabelle e i grafici (senza modificare gli endpoint).
4. Per tutte le altre pagine: mantieni la loro funzionalità originale ma applica lo **stile globale** (colori, card, tabelle) per coerenza visiva.

[ESEMPIO DI ADATTAMENTO DATI – PNG vs API reali]
- Il PNG mostra 3 host, con `PC_DL_ANDREA` DOWN e `pasqui_pc` WARNING.
- Le API reali restituiranno dati dinamici. Tu **non devi mockare nulla**, devi solo mappare i campi restituiti dall'API nei campi visualizzati.
- Se un campo manca (es. `Last Change` non esiste nell'API attuale), usa un campo simile (`timestamp` o `last_alert`) e mostra un formato relativo (es. "2h ago").

[COSTRUZIONE DEL PROMPT CHE DEVI SEGUIRE – per Cursor]
```cmd
/refactor-ui --scope=frontend-only --target=Overview --layout=MISAT